- provide(:title, @category.title)
- provide(:description, @category.description)
= render 'layouts/header', categories: @categories
= render 'layouts/photos', photos: @photos
= render 'layouts/footer'
